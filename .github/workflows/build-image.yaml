name: Build & Push to Container Registry

permissions:
  contents: read
  packages: write

on:
  push:
    branches:
      - master

jobs:
  docker:
    uses: NezuChan/workflows/.github/workflows/docker-build.yml@main
    secrets: inherit
    